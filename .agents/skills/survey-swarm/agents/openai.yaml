interface:
  display_name: "Survey Swarm"
  short_description: "Repo-specific patterns and workflows for survey-swarm"
  default_prompt: "Use the survey-swarm repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true